function eachKeys(func, obj) {
  var keys = Object.keys(obj);
  var eachF = function (key) {
    var value = obj[key]
    func(key, value);
  };

  return keys.forEach(eachF);
}

module.exports = eachKeys;
