function reduce(func, arr, initial) {
  var run = function (previous, current) {
    return func(previous, current);
  };

  return initial ? arr.reduce(run, initial) : arr.reduce(run);
}

module.exports = reduce;
