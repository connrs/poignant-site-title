var templateStream = require('app/stream/template-stream');

module.exports = function templateStreamBuilder (layout, template) {
  return function builder() {
    return new templateStream({
      layout: layout,
      viewTemplate: template
    });
  }
};
